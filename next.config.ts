import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["img.clerk.com", "images.unsplash.com", "your-api.com"], // Add your image domains here
  },
};

export default nextConfig;
